git pull origin develop  :  develop 브랜치의 데이터를 지금 연결된 로컬저장소에 가져옴. (동기화)

git remote -v  :  지금 연결된 원격저장소(리파지토리)가 어떤 것인지 확인할 수 있음.
git remote remove origin  :  지금 연결되어 있는 원격저장소의 연결을 끊는다.

git init  :  초기화 작업 (처음 리파지토리 만든 후 로컬저장소의 데이터를 원격저장소에 올릴 경우 초기화 작업)
             기존에 작업하고 있는 저장소일 경우, 이 명령어를 쓰지 않는다.

git add .  :  로컬저장소에 데이터를 임시저장한 상태

git status  :  로컬저장소의 데이터 변경사항이나 상태를 확인할 수 있음.

git commit -m "커밋 메시지"  :  로컬저장소에 데이터가 진짜로 저장되며, 나중에 
                                원격저장소로 올라가면 커밋메시지가 나타난다.

git push origin BKM  :  BKM 브랜치(원격저장소)에 로컬저장소의 데이터를 보내준다. 
